# Dependency directories (ignore all node_modules)
node_modules/
backend/node_modules/
frontend/node_modules/

# Production compilation directories
frontend/dist/

# Local database files (ignore runtime data so it starts fresh or isn't overwritten by git pushes)
backend/data/events.json
backend/data/users.json

# Environment configuration
.env
.env.local

# OS system files
.DS_Store
Thumbs.db
